class circle:
  def area(r):
    area=math.pi*r*r
    return area
  def paremeter(r):
    p=2*math.pi.r
    return p

radius=float(input("Enter radius "))
circle1=cirlce()
print("Arear is",circle1.area(radius))
print("Paremeter is",circle1.paremeter(radius))
